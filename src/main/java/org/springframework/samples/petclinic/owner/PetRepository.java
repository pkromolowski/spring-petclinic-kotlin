

```cpp
#include <vector>
#include <string>

namespace org {
    namespace springframework {
        namespace samples {
            namespace petclinic {
                namespace owner {

                    class PetRepository {
                    public:
                        std::vector<PetType> findPetTypes() {
                            // Generate later PetType
                        }

                        Pet findById(int id) {
                            // Generate later Pet
                        }

                        void save(Pet pet) {
                            // Generate later Pet
                        }
                    };

                } // owner
            } // petclinic
        } // samples
    } // springframework
} // org
```
